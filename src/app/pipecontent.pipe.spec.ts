import { PipecontentPipe } from './pipecontent.pipe';

describe('PipecontentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipecontentPipe();
    expect(pipe).toBeTruthy();
  });
});
